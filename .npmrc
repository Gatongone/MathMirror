# The `obsidian` typings package pins an exact @codemirror/state peer version,
# which conflicts with the newer CodeMirror releases used by the test suite.
# Obsidian supplies CodeMirror at runtime, so these dev dependencies only matter
# for type checking and for running the tests.
legacy-peer-deps=true
